fn main() {
    let mut count = 0;
    let mut number = 1;

    while number <= 50 {
        count += 1;

        if number * 2 > 30 {
            println!("Condition met at number = {}", number);
            break;
        }

        number += 1;
    }

    println!("Total iterations: {}", count);
}
